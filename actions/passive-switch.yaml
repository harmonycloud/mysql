apiVersion: middleware.cn/v1
kind: MiddlewareActionBaseline
metadata:
  name: mysql-passive-switch
  annotations:
    alias: "自动切换"
spec:
  baselineType: "OpsAction"
  actionType: "passive-switch"
  necessary:
    passiveSwitched: '{"type":"boolean","label":"是否开启自动切换","required":true,"default":true,"visible":true}'
  steps:
    - name: passive-switch
      cmd:
        command:
          - "kubectl"
          - "patch"
          - "mid"
          - "{{ .Globe.MiddlewareName }}"
          - "-n"
          - "{{ .Necessary.Namespace }}"
          - "--type='json'"
          - '-p=''[{"op": "replace", "path": "/spec/parameters/passiveSwitched", "value": {{ .Necessary.passiveSwitched }} }]'''
